{
    "name": "agurodriguez/find-your-service",
    "description": "",
    "require": {
        "ext-mbstring": "*"
    },
    "scripts": {
        "compile": [
            "composer install -d src/api",
            "cd src/api && php artisan db:seed && cd ../.."
        ]
    }
}